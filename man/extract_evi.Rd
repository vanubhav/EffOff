% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_gee_data.R
\name{extract_evi}
\alias{extract_evi}
\title{Extract EVI Data from Google Earth Engine}
\usage{
extract_evi(sf_object, start_year = 1990, end_year = 2024, scale = 30)
}
\arguments{
\item{sf_object}{An sf object containing the region of interest (polygons).}

\item{start_year}{Numeric. The start year for data extraction. Default is 1990.}

\item{end_year}{Numeric. The end year for data extraction. Default is 2024.}

\item{scale}{Numeric. The scale in meters for the reduction. Default is 30.}
}
\value{
A dataframe containing the extracted EVI data.
}
\description{
This function extracts Enhanced Vegetation Index (EVI) data from Landsat 5, 7, and 8
for a given spatial feature (sf object). It harmonizes the data across sensors
and returns the raw pixel values.
}
\details{
This function uses \code{rgee::ee_as_sf(..., via = "drive")} to handle large datasets.
\strong{IMPORTANT:}
\itemize{
\item You must have authenticated Google Drive with \code{rgee} (run \code{ee_Initialize(drive = TRUE)}).
\item This package requires access to Google Drive and other Google services via the \code{googledrive} and \code{tidyverse} packages for automated file handling.
\item An active internet connection is required for this function to communicate with Google Earth Engine and Google Drive.
}

\strong{WARNING:} Extracting pixel-level data for large regions or long time series can generate
massive datasets. If the process fails or takes too long, consider splitting your region
into smaller polygons. The program will periodically check for the completed file in your
Google Drive and download the file when it becomes available.
}
